# ------------------------------------
# 1. Monorepo 依赖管理相关 (PNPM Specific)
# ------------------------------------

# 核心：忽略 pnpm 的虚拟存储（依赖硬链接的实际存储位置）
# 这是一个非常重要的目录，不应该被提交
.pnpm-store

# 忽略每个 workspace 或项目根目录下生成的虚拟依赖目录
# 尽管 pnpm 推荐在根目录安装依赖，但仍可能在子包中生成
/node_modules
**/node_modules

# 根目录下的 .pnpm 虚拟存储目录 (如果设置了 store-dir: .pnpm)
# 如果你没有在 .npmrc 中特别配置，这一行可能不需要，但保留更安全。
.pnpm

# pnpm 的锁文件，用于记录依赖版本。这个文件必须被提交！
# 如果你不需要提交锁文件，可以解除注释，但不推荐：
# pnpm-lock.yaml


# ------------------------------------
# 2. 构建产物与缓存 (Build & Cache)
# ------------------------------------

# 忽略所有包内的构建产物目录
# 约定所有包的构建产物都放在 dist/ 或 lib/
**/dist
**/lib
**/build
**/out

# 忽略所有包的本地开发缓存
**/.turbo           # Turborepo 缓存 (如果使用)
**/.vite            # Vite 缓存
**/.parcel-cache    # Parcel 缓存 (如果使用)
**/.next             # Next.js 产物 (如果使用)
**/.nuxt             # Nuxt.js 产物 (如果使用)
**/.cache


# ------------------------------------
# 3. IDE 和操作系统文件
# ------------------------------------

# IDE / 编辑器文件
.vscode/             # VS Code 配置（如果不需要共享工作区设置）
.idea/               # JetBrains IDE 配置
*.log

# 操作系统文件
.DS_Store
Thumbs.db

# ------------------------------------
# 4. TypeScript / JavaScript 运行环境
# ------------------------------------

# TypeScript 编译的临时文件
*.tsbuildinfo

# 调试文件
.nyc_output/
coverage/

# 环境变量文件
.env
.env.local
.env.*.local

# ------------------------------------
# 5. Changesets (如果使用)
# ------------------------------------

# Changesets 生成的 .json 文件（通常不需要忽略，除非是 CI 过程中生成的临时文件）
# 推荐提交 .changeset/*.md 文件